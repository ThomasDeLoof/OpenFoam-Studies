FoamFile { version 2.0; format ascii; class dictionary; object controlDict; }

application     icoFoam;
startFrom       startTime;
startTime       0;
stopAt          endTime;
endTime         20;      // total simulation time

// time step control
adjustTimeStep  yes;
// maximum stability limit (max CFL number)
// for icofam/pimpleFoam, 0.5 is a safe value, for Navier-Stokes solvers its often set to 1
maxCo           1;
// maximum time step size (for safety, to avoid too large time steps in case of high velocities)
maxDeltaT       0.5;

// initial time step (OpenFOAM will adjust it immediately after the first step)
deltaT          0.05;

writeControl    runTime;
writeInterval   1;      // time step for saving results